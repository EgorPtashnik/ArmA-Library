/**
 * Boot script — must be loaded LAST, after core + all doc / route modules.
 * Runtime overrides (delays, etc.) can also live here.
 */

// ArmADocs.setConfig({ redirectDelayMs: 800 });

ArmADocs.start();
